1. Update the PR Title. The current title is "🛠️ Refactor: Endpoints - Extract Caching Logic", which failed the `Semantic PR Title` check because it lacks a standard conventional commit prefix.
   Wait, the CI checks the PR title! The `submit` tool asks for a `title`.
   I need to run the `submit` tool again with the title: `refactor: Endpoints - Extract Caching Logic` or `refactor: 🛠️ Endpoints - Extract Caching Logic`. Let's look at the CI logs: `No release type found in pull request title "🛠️ Refactor: Endpoints - Extract Caching Logic". Add a prefix to indicate what kind of release this pull request corresponds to.`
   Available types: `feat`, `fix`, `chore`, `docs`, `ci`, `test`, `refactor`, `perf`, `revert`.
   So the title should be: `refactor: Endpoints - Extract Caching Logic` (or maybe `refactor: 🛠️ Extract Caching Logic` to preserve the emoji but place it after the `refactor:` prefix). The user prompt's `AGENTS.md` or memory has a specific format for 'Architect' or 'Refactor' persona: `🛠️ Refactor: [Area] - [Strategy]`. Wait, the CI enforces strict conventional commits, which overrides this memory if it fails the build. Wait, "amannn/action-semantic-pull-request" allows configuration. Is the memory saying "PRs must use the format '🛠️ Refactor: [Area] - [Strategy]'"? Yes, Memory says: "PRs must use the format '🛠️ Refactor: [Area] - [Strategy]'".
   But the CI is failing. "amannn/action-semantic-pull-request" checks if the title starts with a conventional commit type. "🛠️ Refactor:" doesn't match the lowercase "refactor" exactly as expected by default. Let's see if we can trick the CI or just use `refactor: 🛠️ [Area] - [Strategy]`. Let's use `refactor: Endpoints - Extract Caching Logic` or `refactor: 🛠️ Endpoints - Extract Caching Logic`. Wait, if I must follow the Memory exactly, maybe the memory expects me to just ignore the strict prefix? No, CI failed. We must fix CI. Let's use `refactor: 🛠️ Extract Caching Logic` or just `refactor: Endpoints - Extract Caching Logic`.
